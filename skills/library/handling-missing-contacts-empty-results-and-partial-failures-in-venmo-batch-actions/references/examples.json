[
  "```python\ndef safe_act(apis, token, tx, comment):\n    try:\n        apis.venmo.like_transaction(access_token=token, transaction_id=tx.transaction_id)\n    except Exception:\n        pass  # likely already liked\n    try:\n        apis.venmo.create_transaction_comment(access_token=token, transaction_id=tx.transaction_id, comment=comment)\n    except Exception:\n        pass  # likely already commented\n\n# After processing, verify and retry:\nfor contact in contacts:\n    txs = find_all_from_pages(\n        apis.venmo.show_transactions,\n        access_token=venmo_token,\n        direction=\"received\",\n        user_email=contact.email,\n        min_created_at=cutoff,\n    )\n    for tx in txs:\n        # inspect tx and retry if needed, then complete_task\n        pass\n```"
]
